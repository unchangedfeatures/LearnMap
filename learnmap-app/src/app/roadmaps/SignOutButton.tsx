/**
 * Deprecated: sign out lives in the AppShell topbar profile menu.
 *
 * Kept temporarily to avoid breaking imports during refactors.
 * Remove once confirmed unused.
 */
export default function SignOutButton() {
  return null;
}
